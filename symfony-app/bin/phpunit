#!/usr/bin/env php
<?php

declare(strict_types=1);

$phpunit = dirname(__DIR__) . '/vendor/bin/phpunit';

if (!is_file($phpunit)) {
    fwrite(STDERR, "PHPUnit not found. Run composer install first.\n");
    exit(1);
}

require $phpunit;
